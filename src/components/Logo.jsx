export const Logo = ({ onClick }) => {
  return (
    <h1
      onClick={onClick}
      className="tracking-tighter font-bold text-xl text-primary/90 cursor-pointer"
    >
      Arman
    </h1>
  );
};
